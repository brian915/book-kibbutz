<%= simple_form_for(@share) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :user %>
    <%= f.association :book %>
    <%= f.input :sent %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
